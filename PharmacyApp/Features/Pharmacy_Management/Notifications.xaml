<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PharmacyApp.Features.Pharmacy_Management.Notifications"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PharmacyApp.Features.Pharmacy_Management"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ListView ItemsSource="{x:Bind ViewModel.Notifications}" Width="500">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="local:NotificationViewModel">
                <StackPanel Orientation="Vertical" Spacing="30" Padding="15">
                    <TextBlock Text="{x:Bind NotificationTitle}" FontSize="20" VerticalAlignment="top"></TextBlock>
                    <TextBlock Text="{x:Bind NotificationBody}" VerticalAlignment="bottom"></TextBlock>
                    <Button Content="{x:Bind NotificationButtonText}" Click="OnNotificationButtonClicked"></Button>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>
</Page>
